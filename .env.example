# AI Admissions Platform - Backend Environment Configuration
# Copy this file to .env and fill in real values for local development.
# Never commit a real .env file.

# --- Application ---
APP_ENV=development
APP_DEBUG=true
APP_NAME="AI Admissions Platform"
API_V1_PREFIX=/api/v1

# --- Database (PostgreSQL + pgvector) ---
DATABASE_URL=postgresql+psycopg://admissions:admissions@localhost:5432/admissions
DATABASE_URL_TEST=postgresql+psycopg://admissions:admissions@localhost:5432/admissions_test

# --- Security / Auth ---
JWT_SECRET_KEY=change-me-in-production-32-bytes-minimum
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=14

# --- CORS ---
CORS_ALLOWED_ORIGINS=http://localhost:3000

# --- Logging ---
LOG_LEVEL=INFO

# --- Embeddings / RAG ---
EMBEDDING_PROVIDER=local
EMBEDDING_MODEL=local-hashing-v1
EMBEDDING_DIMENSIONS=256
RAG_RELEVANCE_THRESHOLD=0.15
RAG_TOP_K=5
KNOWLEDGE_MAX_FILE_SIZE_MB=15

# --- AI Agent / LLM ---
# The agent's core reasoning (intent detection, tool selection, grounded
# facts) is fully deterministic and never calls an LLM. AGENT_LLM_PROVIDER
# controls ONLY a single bounded fallback (Task 016): naturally phrasing
# a reply when nothing else matched. "mock" (default) needs no key and
# reproduces the exact static template every test expects; "anthropic"
# requires LLM_API_KEY; "gemini" requires GOOGLE_API_KEY; "groq" requires
# GROQ_API_KEY.
AGENT_LLM_PROVIDER=mock
LLM_API_KEY=
LLM_MODEL=claude-sonnet-4-5-20250929
LLM_API_BASE_URL=https://api.anthropic.com
LLM_TIMEOUT_SECONDS=8.0
GOOGLE_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_API_BASE_URL=https://generativelanguage.googleapis.com
AGENT_MAX_TOOL_CALLS_PER_TURN=6

# --- Groq STT/LLM (fast cloud inference, OpenAI-compatible API) ---
# GROQ_API_KEY is read only from this backend environment file - never
# commit a real value, never send it to the frontend. Required when
# STT_PROVIDER=groq and/or AGENT_LLM_PROVIDER=groq below. TTS has no Groq
# adapter - TTS_PROVIDER stays "local" (Piper/MMS).
GROQ_API_KEY=
GROQ_API_BASE_URL=https://api.groq.com
GROQ_STT_MODEL=whisper-large-v3-turbo
GROQ_LLM_MODEL=openai/gpt-oss-20b
GROQ_VOICE_TIMEOUT_SECONDS=8.0

# --- Future providers (not required for this stage) ---
STT_API_KEY=
TTS_API_KEY=
VOICE_PROVIDER_API_KEY=
STORAGE_ACCESS_KEY=
STORAGE_SECRET_KEY=

# --- Voice (Task 011): defaults to the deterministic mock provider - no
# credentials required for local development or tests. STT_PROVIDER also
# accepts "gemini", "local", or "groq" (requires GROQ_API_KEY above). ---
STT_PROVIDER=mock
TTS_PROVIDER=mock
VOICE_TRANSPORT_PROVIDER=mock
TELEPHONY_PROVIDER=mock
TELEPHONY_WEBHOOK_SECRET=

# --- Twilio phone-voice adapter (optional - independent of TELEPHONY_PROVIDER
# above; leave all blank unless you are wiring up a real Twilio number) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
# Must be a real, publicly reachable wss:// URL ending in
# /api/v1/voice/twilio/stream (e.g. via ngrok in local development).
TWILIO_STREAM_PUBLIC_URL=

VOICE_SESSION_MAX_DURATION_SECONDS=1800
VOICE_SESSION_IDLE_TIMEOUT_SECONDS=300
VOICE_MAX_CONCURRENT_SESSIONS_PER_COLLEGE=20

# --- Realtime web voice transport (Task 015) ---
# Set VOICE_TRANSPORT_PROVIDER=livekit and all three of these to use real
# LiveKit infrastructure. Leaving VOICE_TRANSPORT_PROVIDER=mock (default)
# requires none of them. In production, selecting "livekit" without all
# three set fails application startup rather than silently using mock.
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
LIVEKIT_TOKEN_TTL_SECONDS=600

# --- Realtime voice agent worker (Task 016) ---
# A separate process (python -m app.voice.worker.run) that joins a
# LiveKit room, performs STT, drives the existing AgentOrchestrator, and
# publishes synthesized speech back. Irrelevant/unused while
# VOICE_TRANSPORT_PROVIDER=mock (the default) - it simply has nothing to
# dispatch to.
LIVEKIT_WORKER_IDENTITY=admissions-agent
VOICE_WORKER_POLL_INTERVAL_SECONDS=2.0
VOICE_WORKER_SAMPLE_RATE=48000
VOICE_WORKER_CHANNELS=1
